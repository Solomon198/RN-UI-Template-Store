import SearchHeader from './search.header/header.search';
/**
 * - `SearchHeader` *JSX.Element* -  Header component with search bar
 * ---
 */
const headerComponents = {
  /**
   * `placeholder` *string* - place holder of search header component
   */
  SearchHeader,
};
export default headerComponents;
